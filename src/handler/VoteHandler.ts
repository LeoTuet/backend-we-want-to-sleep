import {VoteService} from "../services/VoteService"

const voteService = new VoteService()

export class VoteHandler {
    public saveVote() {


        // voteService.checkIfTokenIsValid()
        // voteService.saveVote()
    }
}